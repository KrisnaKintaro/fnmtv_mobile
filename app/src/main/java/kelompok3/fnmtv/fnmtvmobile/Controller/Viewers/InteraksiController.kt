package kelompok3.fnmtv.fnmtvmobile.Controller.Viewers

class InteraksiController {
    // Fungsi untuk menambahkan komentar
    fun tambahKomentar(beritaId: Int, userId: Int, isiKomentar: String) {
        // Logika INSERT INTO komentar
    }

    // Fungsi untuk memberikan reaksi (like, love, dll)
    fun beriReaksi(beritaId: Int, userId: Int, jenisReaksi: String) {
        // Logika INSERT/UPDATE reaksi
    }
}